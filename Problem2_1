def fib_sum(limit):
    a, b = 1, 2
    fib_sum = 0

    for i in range(1, limit):
        if a % 2 == 0 and a <= 4000000:
            fib_sum += a
        a, b = b, a + b

    return fib_sum

result = fib_sum(100)
print(result)
